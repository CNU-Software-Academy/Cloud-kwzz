1. Image Registry
AWSCLI(화면) - docker install - image pull - image 수정(ip 변경을위한 서비스 생성) - image push

2. Image Build
SuperCloud(화면) - secret(imagepullsecret) 생성 - deploy 생성 - LB ip로 접속해 확인 (+db용 volume 추가할 수 있으면 좋을 거 같다 생각하긴함) - 디플로이 수정으로 파드 수 변경해도 ip 연결 유지 확인

3. Monitoring
Grafana(화면) - 각 pod(container/deploy) cpu usage 체크 - pod 부하(stress) 테스트 - cpu usage 변경 확인
